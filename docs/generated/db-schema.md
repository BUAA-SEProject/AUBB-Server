# 数据库结构

## 当前状态

目前还没有提交任何 Flyway 迁移，因此还没有属于应用自身、可供记录的 PostgreSQL 模式。

## 预期方向

- 关系型状态应存放在 PostgreSQL 中
- 每一次模式变更都必须通过 Flyway 进入仓库
- MyBatis-Plus 应映射到由迁移维护的模式，而不是运行期临时创建的结构

当第一份迁移落地后，用自动生成的模式说明替换这个占位文档。
