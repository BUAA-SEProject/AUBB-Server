# 平台基线

## 目标

为未来的 AUBB 业务能力提供稳定的后端基础。

## 验收标准

- 服务能够在 Java 25 上启动
- 服务暴露 `/actuator/health`
- 仓库文档说明了后续工作该如何规划与验证
- PostgreSQL、RabbitMQ、Redis 的集成路径已经准备好，但尚未产品化
- 在第一个依赖持久化的功能出现前，数据库和 Flyway 自动配置保持延后状态

## 非目标

- 交付终端用户可见的业务功能
- 定义租户、用户或角色模型
- 暴露无需认证的业务 API
